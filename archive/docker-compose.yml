services:
  portfolio:
    build: .
    ports:
      - "3000:80"   # visit http://localhost:3000
    volumes:
      # Mount src/ so changes appear instantly without rebuilding
      # Remove this volume in production (or CI) for a clean build
      - ./src:/usr/share/nginx/html:ro
